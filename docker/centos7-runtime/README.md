```build.sh``` installs Intel MPI and sets up ssh.

```run.sh``` is script to install wrapper and run Molpro inside a container.

```start.sh``` is script to start two containers running sshd enable multi-container execution.
